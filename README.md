# my-website
tinapeng.ca

- Page text: `src/App.jsx`. Meta tags and structured data: `index.html`.
- Build: `npm run build` (Vite build, then prerender into `dist/index.html` so search engines see the page text).
- Netlify settings live in `netlify.toml` (build `npm run build`, publish `dist`).
- Share image: replace `public/og-image.jpg` (1200x630).
